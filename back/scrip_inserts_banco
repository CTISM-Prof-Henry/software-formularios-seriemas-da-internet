import json

from categoria.models import Categoria


with open('categorias.json', 'r', encoding='utf-8') as f:
    dados = json.load(f)
       id=item.get('id'),
        defaults={
            'nome_categoria': item.get('nome'),
            'descricao_categoria': item.get('descricao'),
        }
    )
    if criado:
        print(f"Categoria '{categoria.nome}' cadastrado com sucesso!")
    else:
        print(f"Categoria '{categoria.nome}' já existia no banco.")

exit()
